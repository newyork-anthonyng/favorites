Instructions on how to host on gh-pages

```
// create new branch
$ git checkout --orphan gh-pages

// make initial commit
$ git commit

$ git push origin gh-pages
```

The website should be hosted on https://newyork-anthonyng.github.io/myProjectName
